# Traitty LOG 交付包（規格／組裝包）

> **外包團隊請先讀 `00_外包交接說明.md`**——職責邊界（你只組裝、不改動語意）、資料流、驗收 DoD、常見誤區。本 README 是檔案地圖。
>
> 本 README 檔名與版本必與目錄一致。
> **本包定位：規格與組裝交付包**——含 System 規範全文、打包程序、題庫、內容正本、資料工件與**組裝樣式示意範例**。
> **不含**最終回答樣本：兩份範例的 [SYSTEM PROMPT] 為完整可執行版，但其「回答」由「上線前複測」工作包（Codex 生成＋Gemini 品質評分）產出後另附，不在此包。

## 上手文件（實作團隊先讀這兩份）

| 序 | 檔案 | 說明 |
|---|---|---|
| 00 | `00_外包交接說明.md` | **最先讀**：程式輸入→輸出契約（三類輸入對應）、職責邊界、驗收 DoD、常見誤區、升級路徑 |
| 01 | `01_設計總說明與決策定案_v2_20260728.md` | 為什麼這樣設計＋決策定案紀錄（取代舊 `runtime_payload_spec_proposal` 草案） |
| 02 | `02_參考程式碼_打包器骨架.py` | 參考實作：b §2/§3/§5/§6/§7/§8 邏輯與 §6 四項單元檢查、audience 閘門、欄名正規化、everyday 白名單；xlsx 讀取與 LLM 呼叫標 `TODO(外包)`；含 API 取分數 adapter。已用 mock 資料實跑（含針對性回歸測試）。**外包仍須以真實資料重跑 DoD** |
| — | `LOG檔版式區塊說明_v2_20260728.docx` | **人閱版式說明（給 SME／主管／外包 PM）**：一份自足文件講完三層結構、S1/S2 全文、D1–D4 區塊格式、選列規則、驗證與檔案對照。⚠ 取代 `LOG檔版式區塊說明_20260727.docx`（v1.2，含已推翻的 25/40 上限等，勿再使用） |
| 03 | `03_API對接說明_TalentChatV2.md` | 打包程式輸入從 Talent Chat API v2 怎麼拿：人選＋分數（`/candidates/{id}/report` 的 `trait_id/band`）欄位對接、選題模組、認證、端到端流程、交付前待驗清單 |

## 四件套（規格與內容正本）

| 件 | 檔案 | 說明 |
|---|---|---|
| a. LOG 完成版模板 | `a_LOG完成版模板_v2_20260727.md` | System 靜態規範全文（角色引導 6 條＋全域輸出規範 20 條＋校準規則）＋組裝後區塊樣式 |
| b. 打包規則 | `b_打包規則_v2_20260727.md` | 動態篩選程序（含 §1.1 輸入 schema、§8 完整性檢查、§9 everyday 判定規則）；零語意邏輯 |
| c. 題庫（指令正本／人閱視圖） | `c_快速提問重構版_v9_20260728.xlsx` | 22 題：**指令文字以本檔為正本**（用戶維護）；匡列欄位＋型別＋expected_sections。**程式實際讀 `question_injection_table_v9.json`**，兩者已同步（2026-07-28 驗證一致） |
| d. 內容正本 | `0722 02_08 V6.2 spec_.xlsx` | 02 特質欄位＋08 交互敘事（修復紅標可追）；runtime 資料以此重建 |

## 資料工件（程式引用）

- `question_injection_table_v9.json` — 題目→注入集合＋風險端點 71 條＋清理後指令＋輸入契約
- `風險端點表_定案_20260727.md` — 風險端點人閱視圖（71 條，含裁讀理由；property_peak 兩端已納入）
- `traits_113_v6_2.json` — 113 特質 ID／名稱／band 標籤
- `regex_pack_v6_2.json` — 匯出剝除正則（含 08 開頭句剝除）
- `exit_scanner_wordlist_v6_2.json` — 出口掃描詞表（含 HR 決策／中文分區／人口統計／誠信定罪攔截；everyday 白名單）
- `construct_families_v1.json` — 構念家族表（**僅供稽核**；家族補全已在資料生成期完成，打包層不做）

## 組裝樣式範例（示意；回答另由複測產出）

- `新版LOG範例_匡列型_壓力題_v7.txt` — 匡列型（單人）組裝樣式（[SYSTEM PROMPT] 為 a 文件 System 全文）
- `新版LOG範例_全人型_雙測驗_v7.txt` — 全人型（單人做兩份測驗，交互全注入）組裝樣式
- `新版LOG範例_多人型_會議團隊_v7.txt` — **多人型**組裝樣式（打造高效會議團隊；2 位受測者各一個 `### [受測者]` 區塊、任務指令用多人欄；示範 b §5 多人分隔）

## 實作順序

1. 以 d 重建 runtime 資料 → 2. 照 b 實作打包 → 3. System 文字取自 a → 4. 題目取自 c／`question_injection_table_v9.json` → 5. 掛出口掃描與 §8 完整性檢查 → 6. 紅隊實測（內部標記殘留 0 才放行）。

## 交付前未結項（外包／內容方需處理）

| # | 項目 | 誰處理 | 影響 |
|---|---|---|---|
| 1 | **API `band` 值域未實測**——schema 只標 string，我方系統吃 A/B/C | 外包＋API 方（需憑證） | 若非 A/B/C，adapter 要加轉換（03 §5） |
| 2 | **`module_id ↔ 題目` 對照表未建**——API 用英文碼，題庫用 `idx`／中文 title | 內容方＋API 方 | 未補前選題無法自動對接（03 §3） |
| 3 | **正本 SPA 53 列 `ai_do`／`ai_dont` 缺欄名前綴**（ANI/CIA/CSR 全含） | 內容方 | 程式已用正規化補上；資料面根治仍建議修正本（b §2） |
| 4 | **紅隊實測未跑**——內部標記殘留 0 的放行標準 | 外包 | DoD 4 未驗（需實際 LLM 呼叫） |

## 重要裁定摘要

- 交互選列無「題意截斷上限」（過濾即預算；全人型三步的補充10／防呆90 為結構參數）。
- 回答長度：題庫題不限（架構管）、自由提問一千字以內。
- 校準只適用社會期望反應族（CIA_33／ANI_23／SPA_12），由資料自帶＋靜態規則送達。
- 風險端點含 property_peak 特質兩端（CIA_27／SPA_09 的 A 與 C 皆評）。
- 不採「LLM 自我確認已讀」式檢查（見 b §8）。
