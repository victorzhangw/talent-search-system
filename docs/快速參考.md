# 快速參考卡

## 🚀 一鍵操作

```cmd
# 啟動系統
start_all.bat

# 停止系統
stop_all.bat
```

## 🌐 重要網址

| 服務     | 網址                        |
| -------- | --------------------------- |
| 前端介面 | 瀏覽器自動開啟              |
| 後端 API | http://localhost:8000       |
| API 文檔 | http://localhost:8000/docs  |
| API 測試 | http://localhost:8000/redoc |

## 📁 核心檔案

| 檔案                              | 說明           |
| --------------------------------- | -------------- |
| `talent-chat-frontend.html`       | 前端介面       |
| `BackEnd/talent_search_api.py`    | 後端 API       |
| `BackEnd/private-key-openssh.pem` | SSH 私鑰       |
| `connections.xml`                 | 資料庫連接配置 |

## 🔧 常用命令

### 測試 LLM 連接

```cmd
cd BackEnd
venv\Scripts\activate
python test_llm.py
```

### 測試搜索功能

```cmd
cd BackEnd
venv\Scripts\activate
python test_new_search.py
```

### 查詢資料庫特質

```cmd
cd BackEnd
venv\Scripts\activate
python query_traits.py
```

### 分析資料庫結構

```cmd
cd BackEnd
venv\Scripts\activate
python db_schema_analyzer.py
```

## 💡 搜索範例

```
基礎搜索:
"我需要一個善於溝通的銷售人員"

複雜搜索:
"找一個有創造力、分析能力強、善於團隊合作的產品經理"

特質導向:
"需要協調溝通和積極傾聽能力都很強的人"

職位導向:
"尋找技術創造者，具備 AI 素養和批判性思考"
```

## 🐛 快速排解

### 後端無法啟動

```cmd
cd BackEnd
python -m venv venv
venv\Scripts\activate
pip install -r requirements_api.txt
```

### 前端顯示「未連接」

1. 確認後端正在運行：訪問 http://localhost:8000
2. 檢查瀏覽器控制台（F12）
3. 確認防火牆設定

### SSH 連接失敗

1. 確認私鑰檔案：`BackEnd/private-key-openssh.pem`
2. 確認私鑰格式（OpenSSH，非 PPK）
3. 參考 `BackEnd/使用說明.md`

### LLM API 失敗

```cmd
cd BackEnd
venv\Scripts\activate
python test_llm.py
```

## 📊 系統狀態

### 檢查後端

```
訪問: http://localhost:8000
預期: {"message": "人才聊天搜索 API", ...}
```

### 檢查資料庫

```
後端日誌應顯示:
"SSH 隧道已建立"
"資料庫連接成功"
"載入 50 個特質"
```

### 檢查前端

```
右上角應顯示: 🟢 已連接
```

## 🔑 配置位置

### 資料庫配置

```python
# BackEnd/talent_search_api.py
DB_CONFIG = {
    'ssh_host': '61.216.152.8',
    'ssh_port': 22,
    'ssh_username': 'victor_cheng',
    'ssh_private_key': 'private-key-openssh.pem',
    'db_host': 'localhost',
    'db_port': 5432,
    'db_name': 'projectdb',
    'db_user': 'projectuser',
    'db_password': 'projectpass'
}
```

### LLM 配置

```python
# BackEnd/talent_search_api.py
LLM_CONFIG = {
    'api_key': 'sk-xmwxrtsxgsjwuyeceydoyuopezzlqresdjyvlzrbbjeejiff',
    'api_host': 'https://api.siliconflow.cn',
    'model': 'deepseek-ai/DeepSeek-V3'
}
```

### 前端配置

```javascript
// talent-chat-frontend.html
apiBaseUrl: "http://localhost:8000";
```

## 📚 文檔快速連結

| 需求       | 文檔                            |
| ---------- | ------------------------------- |
| 快速上手   | 快速啟動指南.md                 |
| 詳細說明   | README-啟動說明.md              |
| 架構設計   | 新架構說明-資料庫驅動.md        |
| LLM 原理   | LLM 搜索流程詳解.md             |
| 資料庫特質 | 特質歸類系統說明.md             |
| API 文檔   | BackEnd/README_TALENT_SEARCH.md |
| 專案總結   | 專案總結.md                     |

## 🎯 核心概念

### 資料庫驅動

```
所有特質來自資料庫 trait 表
→ 確保數據一致性
→ 易於擴展
```

### LLM 生成 SQL

```
自然語言 → LLM 分析 → SQL WHERE 條件
→ 精確查詢
→ 高效能
```

### 真實分數匹配

```
基於候選人實際測評分數
→ 精確匹配
→ 可信度高
```

## 📞 獲取幫助

### 文檔

1. 查看 `文件導覽.md` 找到相關文檔
2. 閱讀 `README-啟動說明.md` 的疑難排解

### 測試

1. 運行 `test_llm.py` 測試 LLM
2. 運行 `test_new_search.py` 測試搜索
3. 查看後端日誌

### API 文檔

訪問 http://localhost:8000/docs 查看完整 API

## 🎨 UI 快捷鍵

| 操作       | 快捷鍵   |
| ---------- | -------- |
| 發送訊息   | Enter    |
| 清除聊天   | (按鈕)   |
| 查看候選人 | 點擊卡片 |

## 📊 效能指標

| 指標     | 目標值   |
| -------- | -------- |
| API 響應 | < 2 秒   |
| LLM 解析 | < 1 秒   |
| SQL 查詢 | < 0.5 秒 |

## 🔐 安全提醒

- ⚠️ 不要提交私鑰到版本控制
- ⚠️ 不要在公開場合分享 API Key
- ⚠️ 生產環境使用 HTTPS
- ⚠️ 定期更新密碼

## 💾 備份建議

### 重要檔案

- `BackEnd/private-key-openssh.pem`
- `connections.xml`
- `BackEnd/talent_search_api.py`
- `talent-chat-frontend.html`

### 資料庫

定期備份 PostgreSQL 資料庫

---

**提示**: 將此文件加入書籤，方便快速查閱！
