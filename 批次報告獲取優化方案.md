# 批次報告獲取優化方案

## 🎯 優化目標

**原有問題**：
- ❌ 每次點擊候選人姓名都要呼叫 API
- ❌ 重複獲取相同的報告資料
- ❌ 使用者體驗不佳（每次都要等待載入）

**優化方案**：
- ✅ 點擊「開始提問」時批次獲取所有候選人的報告
- ✅ 將報告儲存在 Session Storage 中
- ✅ 點擊候選人姓名時直接從快取讀取
- ✅ 大幅減少 API 呼叫次數，提升使用者體驗

---

## 🔄 新的資料流程

```
使用者勾選候選人（單選/複選）
    ↓
點擊「開始提問」
    ↓
【關鍵】批次獲取所有候選人的特質報告
    ↓
儲存報告到 Session Storage
    ↓
進入 Chat 模式
    ↓
使用者點擊候選人姓名連結
    ↓
從 Session Storage 讀取報告（快速顯示）
    ↓
如果快取中沒有，才呼叫單一報告 API（Fallback）
```

---

## 📡 新增的 API 端點

### Endpoint
```
POST /api/v2/reports/batch
```

### Request

**Headers**:
```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer {token}"
}
```

**Body**:
```json
{
  "assessment_ids": [62, 63, 64, 61]
}
```

### Response

**成功 (200)**:
```json
{
  "reports": [
    {
      "assessment_id": 62,
      "traits": [
        {
          "name": "同理心",
          "score": 85,
          "band": "High"
        },
        {
          "name": "洞察力",
          "score": 72,
          "band": "Mid"
        }
        // ... 其他特質
      ],
      "assessment_date": "2025-12-03T09:30:48.315395Z"
    },
    {
      "assessment_id": 63,
      "traits": [ /* ... */ ],
      "assessment_date": "2025-12-03T09:30:44.494004Z"
    }
    // ... 其他報告
  ]
}
```

**錯誤 (400)**:
```json
{
  "error": "assessment_ids is required"
}
```

---

## 💻 前端實作細節

### 1. 點擊「開始提問」時批次獲取報告

**檔案**：`ChatContainer.vue`  
**函數**：`handleSelectionConfirmed(ids)`

```javascript
const handleSelectionConfirmed = async (ids) => {
    selectedCandidateIds.value = ids
    
    // 儲存候選人資料到 Session Storage
    const selectedCandidates = candidates.value.filter(c => ids.includes(c.candidate_id))
    sessionStorage.setItem('traitty_selected_candidates', JSON.stringify(selectedCandidates))
    
    // 【新增】批次獲取特質報告
    console.log('[ChatContainer] Fetching batch trait reports for', ids.length, 'candidates...')
    await fetchBatchTraitReports(selectedCandidates)
    
    currentTab.value = 'chat'
    messages.value.push({ 
        role: 'ai', 
        content: `已勾選 ${ids.length} 位候選人。您現在可以針對他們進行提問。` 
    })
}
```

### 2. 批次獲取報告函數

**函數**：`fetchBatchTraitReports(selectedCandidates)`

```javascript
const fetchBatchTraitReports = async (selectedCandidates) => {
    const { apiBaseUrl } = getApiConfig()
    
    // 提取 assessment IDs
    const assessmentIds = selectedCandidates
        .map(c => c.latest_assessment?.assessment_id)
        .filter(id => id != null)
    
    if (assessmentIds.length === 0) {
        console.warn('[ChatContainer] No assessment IDs found')
        return
    }
    
    console.log('[ChatContainer] Fetching reports for assessment IDs:', assessmentIds)
    
    try {
        // 呼叫批次報告 API
        const res = await fetch(`${apiBaseUrl}/reports/batch`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${userToken.value}`
            },
            body: JSON.stringify({
                assessment_ids: assessmentIds
            })
        })
        
        if (!res.ok) {
            throw new Error(`Batch reports fetch failed: ${res.status}`)
        }
        
        const data = await res.json()
        
        // 建立 candidate_id -> report 的對應關係
        const reportsMap = {}
        data.reports.forEach(report => {
            const candidate = selectedCandidates.find(
                c => c.latest_assessment?.assessment_id === report.assessment_id
            )
            if (candidate) {
                reportsMap[candidate.candidate_id] = report
            }
        })
        
        // 儲存到 Session Storage
        sessionStorage.setItem('traitty_batch_reports', JSON.stringify(reportsMap))
        console.log('[ChatContainer] Saved', Object.keys(reportsMap).length, 'reports to Session Storage')
        
    } catch (e) {
        console.error('[ChatContainer] Failed to fetch batch reports:', e)
        // 非阻塞：即使批次獲取失敗，仍可繼續使用（會回退到單一 API）
    }
}
```

### 3. Session Storage 資料結構

**Key**: `traitty_batch_reports`

**Value** (JSON 字串):
```json
{
  "58": {
    "assessment_id": 62,
    "traits": [
      { "name": "同理心", "score": 85, "band": "High" },
      { "name": "洞察力", "score": 72, "band": "Mid" }
    ],
    "assessment_date": "2025-12-03T09:30:48.315395Z"
  },
  "59": {
    "assessment_id": 63,
    "traits": [ /* ... */ ],
    "assessment_date": "2025-12-03T09:30:44.494004Z"
  }
}
```

### 4. Modal 優先從快取讀取

**檔案**：`TraitReportModal.vue`  
**函數**：`onMounted()`

```javascript
onMounted(async () => {
    console.log("TraitReportModal Mounted. ID:", props.candidateId)
    
    // 【優先】從 Session Storage 讀取
    try {
        const cachedReports = sessionStorage.getItem('traitty_batch_reports')
        if (cachedReports) {
            const reportsMap = JSON.parse(cachedReports)
            const cachedReport = reportsMap[props.candidateId]
            
            if (cachedReport) {
                console.log('[TraitReportModal] Using cached report for candidate', props.candidateId)
                traits.value = cachedReport.traits || []
                assessmentDate.value = cachedReport.assessment_date || ''
                loading.value = false
                return  // 提前退出，不需要呼叫 API
            }
        }
    } catch (e) {
        console.warn('[TraitReportModal] Failed to load from cache:', e)
    }
    
    // 【Fallback】如果快取中沒有，才呼叫 API
    console.log('[TraitReportModal] Cache miss, fetching from API...')
    try {
        const res = await fetch(`http://localhost:5000/api/v2/candidates/${props.candidateId}/report`, {
            headers: { 'Authorization': `Bearer ${props.token}` }
        })
        if (!res.ok) throw new Error('Fetch failed')
        const data = await res.json()
        traits.value = data.traits
        assessmentDate.value = data.assessment_date
    } catch (e) {
        error.value = "無法載入報告數據"
    } finally {
        loading.value = false
    }
})
```

### 5. 重選時清空快取

**函數**：`resetAndReselect()`

```javascript
const resetAndReselect = () => {
    // ... 其他重置邏輯 ...
    
    // 清空 Session Storage（候選人資料 + 報告快取）
    try {
        sessionStorage.removeItem('traitty_selected_candidates')
        sessionStorage.removeItem('traitty_batch_reports')  // 【新增】清空報告快取
        console.log('[ChatContainer] Cleared Session Storage')
    } catch (e) {
        console.error('[ChatContainer] Failed to clear Session Storage:', e)
    }
    
    // ... 其他重置邏輯 ...
}
```

---

## 🔧 後端實作細節

### 檔案結構
```
BackEnd/api_v2/
├── routes/
│   ├── reports.py          # 【新增】批次報告路由
│   ├── candidates.py       # 現有的單一報告路由
│   └── ...
└── app.py                  # 註冊 reports 路由
```

### 批次報告路由

**檔案**：`routes/reports.py`

```python
@bp.route('/batch', methods=['POST'])
def get_batch_reports():
    """
    批次獲取特質報告
    Request Body: { "assessment_ids": [62, 63, 64, 61] }
    """
    # 1. 驗證與生成 Token
    user_email = "eva@wepredict.io"
    auth_header = request.headers.get('Authorization')
    # ... 提取 email 邏輯 ...
    upstream_token = generate_upstream_token(user_email)
    service = get_service()
    
    # 2. 獲取 assessment IDs
    data = request.json
    assessment_ids = data.get('assessment_ids', [])
    
    if not assessment_ids:
        return jsonify({'error': 'assessment_ids is required'}), 400
    
    # 3. 批次呼叫上游 API
    if isinstance(service, RealIntegrationService):
        results = service.get_assessments(upstream_token, assessment_ids)
    else:
        results = service.get_assessments(assessment_ids)
    
    # 4. 格式化每個報告
    formatted_reports = []
    for report_data in results:
        # 提取 assessment_id
        asmt_id = report_data.get('assessment_id') or \
                  report_data.get('assessment', {}).get('assessment_id')
        
        # 提取並格式化特質資料
        raw_traits = report_data.get('assessment', {}).get('trait_results', {})
        # ... 格式化邏輯 ...
        
        formatted_reports.append({
            'assessment_id': asmt_id,
            'traits': formatted_traits,
            'assessment_date': assessment_date
        })
    
    return jsonify({'reports': formatted_reports})
```

### 上游 API 呼叫

**Endpoint**: `POST https://uat.traitty.com/v1/assessments/latest:batch`

**Request Body**:
```json
{
  "assessment_ids": ["62", "63", "64", "61"]
}
```

**Response**: 包含所有 assessment 的詳細 `trait_results`

---

## 📊 效能比較

### 原有流程（未優化）
```
選擇 4 位候選人
    ↓
點擊「開始提問」（0 次 API 呼叫）
    ↓
點擊候選人 A 姓名 → API 呼叫 1
點擊候選人 B 姓名 → API 呼叫 2
點擊候選人 C 姓名 → API 呼叫 3
點擊候選人 D 姓名 → API 呼叫 4

總計：4 次 API 呼叫（每次點擊都要等待）
```

### 優化後流程
```
選擇 4 位候選人
    ↓
點擊「開始提問」→ 批次 API 呼叫 1（一次獲取所有報告）
    ↓
點擊候選人 A 姓名 → 從快取讀取（即時顯示）
點擊候選人 B 姓名 → 從快取讀取（即時顯示）
點擊候選人 C 姓名 → 從快取讀取（即時顯示）
點擊候選人 D 姓名 → 從快取讀取（即時顯示）

總計：1 次 API 呼叫（後續點擊即時顯示）
```

### 效能提升
- **API 呼叫次數**：減少 75%（4 次 → 1 次）
- **使用者體驗**：點擊候選人姓名後**即時顯示**報告（無需等待）
- **伺服器負載**：大幅降低

---

## ✅ 優勢總結

### 1. 減少 API 呼叫
- 從「每次點擊呼叫 API」變成「一次批次獲取」
- 對於 N 位候選人，從 N 次呼叫減少到 1 次

### 2. 提升使用者體驗
- 點擊候選人姓名後**即時顯示**報告
- 無需每次都等待載入動畫

### 3. 降低伺服器負載
- 批次呼叫比多次單一呼叫更有效率
- 減少上游 API 的請求次數

### 4. 離線可用性
- 報告儲存在 Session Storage 中
- 即使短暫斷網，仍可查看已快取的報告

### 5. 向後相容
- 如果批次獲取失敗，仍會回退到單一 API
- 不影響現有功能

---

## 🔍 Debug 檢查點

### 前端 Console

**點擊「開始提問」時**：
```
[ChatContainer] Fetching batch trait reports for 4 candidates...
[ChatContainer] Fetching reports for assessment IDs: [62, 63, 64, 61]
[ChatContainer] Saved 4 reports to Session Storage
```

**點擊候選人姓名時**：
```
[TraitReportModal] Using cached report for candidate 58
```

**如果快取失效**：
```
[TraitReportModal] Cache miss, fetching from API...
```

### 後端 Log

```
[Batch Reports] Fetching 4 reports for IDs: [62, 63, 64, 61]
[RealService] Batch Fetch Assessments URL: https://uat.traitty.com/v1/assessments/latest:batch
[RealService] Request Body: {'assessment_ids': [62, 63, 64, 61]}
[Batch Reports] Returning 4 formatted reports
```

### Session Storage 檢查

**開發者工具 → Application → Session Storage**

查看 `traitty_batch_reports` 的內容：
```json
{
  "58": { "assessment_id": 62, "traits": [...], ... },
  "59": { "assessment_id": 63, "traits": [...], ... },
  "60": { "assessment_id": 64, "traits": [...], ... },
  "57": { "assessment_id": 61, "traits": [...], ... }
}
```

---

## 📝 測試步驟

### 1. 測試批次獲取
1. 勾選 2-4 位候選人
2. 點擊「開始提問」
3. 檢查 Console：應該看到批次獲取的 log
4. 檢查 Session Storage：應該有 `traitty_batch_reports`

### 2. 測試快取讀取
1. 點擊第一位候選人姓名
2. 檢查 Console：應該看到 "Using cached report"
3. Modal 應該**即時顯示**（無載入動畫）

### 3. 測試 Fallback
1. 手動清空 Session Storage
2. 點擊候選人姓名
3. 檢查 Console：應該看到 "Cache miss, fetching from API..."
4. 應該正常顯示報告（從 API 獲取）

### 4. 測試重選清空
1. 點擊「重選」按鈕
2. 檢查 Session Storage：`traitty_batch_reports` 應該被清空
3. 重新選擇候選人並開始提問
4. 應該重新批次獲取報告

---

## 🎯 總結

### 呼叫時機
- **批次獲取報告**：點擊「開始提問」時
- **查看個人報告**：點擊候選人姓名連結時（從快取讀取）

### 使用的參數
- **批次 API**：`assessment_ids` 陣列（從 `latest_assessment.assessment_id` 提取）
- **快取 Key**：`candidate_id`（用於從 Session Storage 讀取）

### 資料流程
```
點擊「開始提問」
    ↓
提取 assessment_ids: [62, 63, 64, 61]
    ↓
POST /api/v2/reports/batch
    ↓
後端呼叫 POST /v1/assessments/latest:batch
    ↓
格式化並回傳報告陣列
    ↓
前端建立 candidate_id -> report 對應關係
    ↓
儲存到 Session Storage
    ↓
點擊候選人姓名 → 從快取讀取 → 即時顯示
```
