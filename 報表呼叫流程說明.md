# 勾選候選人後取得報表的流程說明

## 📍 呼叫時機與節點

### 🎯 關鍵：**點擊候選人姓名連結時**才會呼叫取得報表

**觸發位置**：
- **前端檔案**：`ChatContainer.vue` 第 74-81 行
- **觸發事件**：使用者點擊已選中候選人的姓名連結

```vue
<div class="selected-summary">
    已鎖定: 
    <span 
        v-for="(cand, idx) in selectedCandidatesObjects" 
        :key="cand.id"
        class="candidate-link"
        @click="openReport(cand)"  <!-- 這裡觸發 -->
    >
        {{ cand.name }}<span v-if="idx < selectedCandidatesObjects.length - 1">, </span>
    </span>
</div>
```

---

## 🔄 完整流程圖

```
使用者勾選候選人（單選/複選）
    ↓
點擊「開始提問」
    ↓
進入 Chat 模式（顯示已選中的候選人姓名）
    ↓
【關鍵】使用者點擊候選人姓名連結
    ↓
觸發 openReport(cand) 函數
    ↓
設定 currentReportCandidate 和 showReportModal
    ↓
渲染 TraitReportModal 元件
    ↓
Modal 的 onMounted 自動執行
    ↓
發送 GET /api/v2/candidates/{candidate_id}/report
    ↓
後端處理：
  1. 從候選人列表找到該候選人
  2. 提取 latest_assessment.assessment_id
  3. 呼叫 /v1/assessments/latest:batch
  4. 格式化特質資料
    ↓
回傳 JSON 給前端
    ↓
Modal 顯示特質報告表格
```

---

## 📡 API 呼叫詳細資訊

### 1️⃣ 前端觸發函數

**檔案**：`ChatContainer.vue` 第 223-227 行

```javascript
const openReport = (cand) => {
    console.log("openReport called with:", cand)
    currentReportCandidate.value = cand
    showReportModal.value = true
}
```

**參數 `cand` 的結構**：
```javascript
{
  candidate_id: 58,
  id: 58,  // 前端內部使用
  name: "吳至平",
  email: "peterwuzhp@outlook.com",
  phone: "",
  enterprise_name: "WePredict",
  position: "",
  status: "employed",
  created_at: "2025-12-01T14:18:37.437348Z",
  last_assessment_date: "2025-12-03T09:30:48.315395Z",
  latest_assessment: {
    assessment_id: 62,
    project_code: 3,
    project_name: "CIA綜合人才洞察評鑑",
    score_value: 56
  }
}
```

---

### 2️⃣ Modal 元件自動發送 API 請求

**檔案**：`TraitReportModal.vue` 第 73-88 行

**API Endpoint**：
```
GET http://localhost:5000/api/v2/candidates/{candidate_id}/report
```

**Headers**：
```javascript
{
  'Authorization': 'Bearer {userToken}'
}
```

**使用的參數**：
- **`candidateId`**：從 props 傳入（來自 `currentReportCandidate.id`）
- **`token`**：從 props 傳入（來自 `userToken.value`）

**程式碼**：
```javascript
onMounted(async () => {
    console.log("TraitReportModal Mounted. ID:", props.candidateId, "Token:", props.token ? "Yes" : "No")
    try {
        const res = await fetch(`http://localhost:5000/api/v2/candidates/${props.candidateId}/report`, {
            headers: { 'Authorization': `Bearer ${props.token}` }
        })
        if (!res.ok) throw new Error('Fetch failed')
        const data = await res.json()
        traits.value = data.traits
        assessmentDate.value = data.assessment_date
    } catch (e) {
        error.value = "無法載入報告數據"
    } finally {
        loading.value = false
    }
})
```

---

### 3️⃣ 後端處理流程

**檔案**：`BackEnd/api_v2/routes/candidates.py` 第 72-210 行

**路由定義**：
```python
@bp.route('/<candidate_id>/report', methods=['GET'])
def get_candidate_report(candidate_id):
```

#### Step 1: 驗證與生成 Token
```python
# 從 Authorization header 提取使用者 email
user_email = "eva@wepredict.io"  # 預設值
auth_header = request.headers.get('Authorization')
if auth_header and auth_header.startswith('Bearer '):
    incoming_token = auth_header.split(" ")[1]
    decoded = jwt.decode(incoming_token, options={"verify_signature": False})
    user_email = decoded.get('email', user_email)

# 生成上游 API token
upstream_token = generate_upstream_token(user_email)
```

#### Step 2: 找到候選人並提取 Assessment ID
```python
# 從候選人列表中找到目標候選人
if isinstance(service, RealIntegrationService):
    resp = service.get_candidates(upstream_token, limit=100)
    candidates = resp.get('data', [])

# 字串比對找到候選人
target_cand = None
for c in candidates:
    if str(c.get('candidate_id')) == str(candidate_id):
        target_cand = c
        break

# 提取 assessment_id
asmt_id = None
lat = target_cand.get('latest_assessment')
if lat and isinstance(lat, dict):
    asmt_id = lat.get('assessment_id')  # 關鍵參數！
```

**關鍵參數**：`assessment_id`（從 `latest_assessment.assessment_id` 提取）

#### Step 3: 批次呼叫上游 API 獲取報告

**上游 API Endpoint**：
```
POST https://uat.traitty.com/v1/assessments/latest:batch
```

**Request Payload**：
```json
{
  "assessment_ids": ["62"]  // 單一候選人的 assessment_id
}
```

**程式碼**：
```python
if isinstance(service, RealIntegrationService):
    results = service.get_assessments(upstream_token, [asmt_id])
    
    # 從結果中找到對應的報告
    report_data = None
    for r in results:
        if str(r.get('assessment_id')) == str(asmt_id):
            report_data = r
            break
```

#### Step 4: 格式化並回傳

**提取特質資料**：
```python
raw_traits = report_data.get('assessment', {}).get('trait_results', {})

# 正規化為列表
if isinstance(raw_traits, dict):
    iter_traits = raw_traits.values()

# 格式化每個特質
for t in iter_traits:
    tid = t.get('trait_id')
    name = TRAIT_TRANSLATIONS.get(tid) or t.get('chinese_name') or 'Unknown'
    score = t.get('score', 0)
    formatted_traits.append({
        'name': name,
        'score': score,
        'band': t.get('band', '')
    })

# 按分數降序排列
formatted_traits.sort(key=lambda x: x['score'], reverse=True)
```

**回傳 JSON**：
```json
{
  "candidate_name": "吳至平",
  "assessment_date": "2025-12-03T09:30:48.315395Z",
  "traits": [
    {
      "name": "同理心",
      "score": 85,
      "band": "High"
    },
    {
      "name": "洞察力",
      "score": 72,
      "band": "Mid"
    }
    // ... 其他特質
  ]
}
```

---

## 🔑 關鍵參數總結

### 前端使用的參數

| 參數 | 來源 | 用途 |
|------|------|------|
| `candidate_id` | `cand.id` 或 `cand.candidate_id` | URL 路徑參數 |
| `token` | `userToken.value` | Authorization header |

### 後端使用的參數

| 參數 | 來源 | 用途 |
|------|------|------|
| `candidate_id` | URL 路徑參數 | 找到目標候選人 |
| `assessment_id` | `latest_assessment.assessment_id` | **批次呼叫上游 API 的關鍵參數** |
| `upstream_token` | `generate_upstream_token(user_email)` | 上游 API 認證 |

### 上游 API 使用的參數

| 參數 | 值 | 說明 |
|------|------|------|
| `assessment_ids` | `["62"]` | 陣列格式，包含要獲取的 assessment ID |

---

## ⚠️ 重要注意事項

### 1. **不是在勾選時呼叫**
- ❌ 勾選候選人時**不會**立即呼叫報表 API
- ✅ 只有在**點擊候選人姓名連結**時才會呼叫

### 2. **每次點擊都會重新呼叫**
- 沒有快取機制
- 每次打開 Modal 都會重新從 API 獲取資料

### 3. **單一候選人報告**
- 每次只獲取一位候選人的報告
- 即使選中多位候選人，也是逐一點擊查看

### 4. **使用 assessment_id 而非 candidate_id**
- 上游 API `/v1/assessments/latest:batch` 使用的是 **`assessment_id`**
- 需要先從候選人資料中提取 `latest_assessment.assessment_id`

---

## 🔍 Debug 檢查點

### 前端 Console
```javascript
// 檢查點擊的候選人資料
console.log("openReport called with:", cand)

// 檢查傳遞給 Modal 的參數
console.log("candidateId:", props.candidateId)
console.log("token:", props.token ? "Yes" : "No")
```

### 後端 Log
```python
# 檢查收到的 candidate_id
print(f"DEBUG: Looking for candidate_id: {candidate_id}")

# 檢查提取的 assessment_id
print(f"DEBUG: Fetching assessment {asmt_id} for candidate {candidate_id}")

# 檢查上游 API 回傳
print(f"DEBUG: Real service returned results: {results}")
```

---

## 📊 資料流程時序圖

```
時間軸：
T0: 使用者勾選候選人 58, 59
    → 前端儲存 selectedCandidateIds = [58, 59]
    → **沒有呼叫任何 API**

T1: 點擊「開始提問」
    → 進入 Chat 模式
    → 顯示「已鎖定: 吳至平, 阮暐捷」
    → **沒有呼叫報表 API**

T2: 使用者點擊「吳至平」連結
    → 觸發 openReport(cand)
    → 設定 currentReportCandidate = { candidate_id: 58, ... }
    → showReportModal = true

T3: TraitReportModal 元件掛載
    → onMounted 執行
    → **呼叫 GET /api/v2/candidates/58/report**

T4: 後端處理
    → 從候選人列表找到 candidate_id=58
    → 提取 latest_assessment.assessment_id = 62
    → **呼叫 POST /v1/assessments/latest:batch**
    → Payload: { "assessment_ids": ["62"] }

T5: 上游 API 回傳報告資料
    → 後端格式化特質資料
    → 回傳給前端

T6: 前端顯示報告
    → Modal 顯示特質表格
```

---

## 💡 總結

### 呼叫節點
**點擊候選人姓名連結時**（不是勾選時）

### 使用的 Payload 參數

**前端 → 後端**：
- URL 參數：`candidate_id`
- Header：`Authorization: Bearer {token}`

**後端 → 上游 API**：
- Endpoint：`POST /v1/assessments/latest:batch`
- Body：`{ "assessment_ids": ["62"] }`
- Header：`Authorization: Bearer {upstream_token}`

### 關鍵參數
**`assessment_id`**（從 `latest_assessment.assessment_id` 提取）
