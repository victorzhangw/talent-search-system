window.ADMIN_PANEL_CONFIG = {
    // API URL for the Admin Panel Backend
    // Leave empty ("") to use relative path (e.g. /api/...) which works if served via Nginx/IIS proxy.
    // Or set absolute URL like "http://uat-api:5000" if backend is on a different domain/port.
    apiBaseUrl: ""
};
