# 修復 Chat 提問時無法顯示候選人真實姓名的問題

## 問題描述

當使用者在前端選擇候選人後，點擊 candidate-link 可以正確顯示候選人的特質報告，但是在 chat 中提問時，AI 回應中只顯示 "Candidate-XX" 而非真實姓名（如 "Evagelion"、"Devin"）。

## 根本原因分析

### 資料流程比較

**點擊 candidate-link 成功的原因：**
- 前端直接使用 `selectedCandidatesObjects`，這些物件來自前端的 `candidates.value`
- 包含完整的候選人資料：`candidate_id`、`name`、`position` 等

**Chat 提問失敗的原因：**
1. 前端在發送 chat 請求時，只傳送了 `candidate_ids`（陣列）
2. 後端收到 ID 後，需要重新從 Traitty API 獲取候選人基本資料
3. **問題**：Traitty API 回傳的候選人資料可能不包含 `name` 欄位，或欄位結構不同
4. 導致 `context_builder.py` 無法取得候選人姓名，只能顯示 "Candidate-XX"

## 解決方案

### 策略：前端傳送完整候選人資訊

不依賴後端重新從 API 獲取候選人基本資料，而是讓前端在 chat 請求中直接傳送已經載入的完整候選人資訊。

### 優點
1. **減少 API 呼叫**：不需要後端重新獲取候選人資料
2. **資料一致性**：確保前端顯示和後端使用的是相同的候選人資料
3. **更快回應**：減少網路請求時間
4. **避免資料結構差異**：不依賴 API 回傳格式

## 程式碼修改

### 1. 前端修改 (ChatContainer.vue)

**檔案**：`frontend/chat-widget/src/components/ChatContainer.vue`

**修改位置**：第 397-409 行

**修改內容**：
```javascript
// 在 chat 請求中新增 candidates_info 欄位
body: JSON.stringify({
  query: query,
  candidate_ids: selectedCandidateIds.value,
  // 新增：傳送完整的候選人基本資訊
  candidates_info: selectedCandidatesObjects.value.map(c => ({
    candidate_id: c.candidate_id,
    name: c.name,
    position: c.position,
    latest_assessment: c.latest_assessment
  })),
  session_id: currentSessionId.value
})
```

### 2. 後端路由修改 (chat.py)

**檔案**：`BackEnd/api_v2/routes/chat.py`

**修改位置**：第 16-30 行

**修改內容**：
```python
@bp.route('/', methods=['POST'])
def chat():
    data = request.json
    query = data.get('query')
    candidate_ids = data.get('candidate_ids', [])
    candidates_info = data.get('candidates_info', [])  # 新增：接收候選人資訊
    session_id = data.get('session_id', 'default_session')
    
    # ... 略 ...
    
    response_stream, use_case_id = rag_service.generate_response(
        query, candidate_ids, session_id, candidates_info=candidates_info
    )
```

### 3. RAG Engine 修改 (rag_engine.py)

**檔案**：`BackEnd/api_v2/services/rag_engine.py`

**修改位置**：第 84-183 行

**主要修改**：

1. **方法簽名**：新增 `candidates_info` 參數
```python
def generate_response(self, query: str, candidate_ids: List[str], 
                     session_id: str, candidates_info: List[Dict] = None):
```

2. **優先使用前端資料**：
```python
if candidates_info:
    # 使用前端提供的資料（優先）
    print(f"[RAG] Using {len(candidates_info)} candidates from frontend")
    target_candidates_basic = candidates_info
    for c in target_candidates_basic:
        print(f"[RAG-DEBUG] Frontend candidate: id={c.get('candidate_id')}, name='{c.get('name')}', position='{c.get('position')}'", flush=True)
else:
    # 回退：從 API 獲取
    print(f"[RAG] No frontend candidate info. Fetching from API...")
    # ... 原有的 API 獲取邏輯 ...
```

### 4. Context Builder 增強 (context_builder.py)

**檔案**：`BackEnd/api_v2/services/context_builder.py`

**修改位置**：第 38-51 行

**修改內容**：
- 新增 debug 日誌
- 新增 fallback 機制：如果 `name` 欄位為空，使用 `Candidate-{id}` 作為替代
- 新增警告訊息以便診斷

```python
# Debug: Log raw candidate data
print(f"[ContextBuilder-DEBUG] Processing candidate: {cand.get('candidate_id')}, name='{cand.get('name')}', position='{cand.get('position')}'", flush=True)

# Robust name extraction with fallback
name = cand.get('name') or f"Candidate-{cand.get('candidate_id', 'Unknown')}"
position = cand.get('position', 'NA')
cand_name = f"{name} ({position})"

if not cand.get('name'):
    print(f"[ContextBuilder-WARNING] Candidate {cand.get('candidate_id')} has no 'name' field. Using fallback.", flush=True)
```

## Debug 日誌

為了方便診斷問題，在以下位置新增了詳細的 debug 日誌：

1. **rag_engine.py**：
   - 第 127 行：記錄前端提供的候選人資料
   - 第 147 行：記錄從 API 獲取的候選人資料
   - 第 237 行：記錄最終合併後的候選人資料

2. **context_builder.py**：
   - 第 41 行：記錄處理中的候選人資料
   - 第 48 行：警告缺少 name 欄位的候選人

## 測試建議

1. **清除快取**：重新啟動後端服務以清除 RAG cache
2. **重新選擇候選人**：在前端重新選擇候選人並開始提問
3. **檢查日誌**：查看後端 console 輸出，確認：
   - `[RAG] Using X candidates from frontend` 訊息
   - `[RAG-DEBUG] Frontend candidate: ...` 顯示正確的姓名
   - `[ContextBuilder-DEBUG] Processing candidate: ...` 顯示正確的姓名
4. **驗證 AI 回應**：確認 AI 回應中使用真實姓名而非 "Candidate-XX"

## 向後相容性

此修改保持向後相容：
- 如果前端沒有傳送 `candidates_info`，後端會自動回退到原有的 API 獲取邏輯
- 不影響現有的 candidate-link 功能
- 不影響快取機制

## 預期效果

修改後，當使用者在 chat 中提問時：
- AI 會使用候選人的真實姓名（如 "Evagelion"、"Devin"）
- 不會再出現 "Candidate-XX" 的佔位符
- 回應內容會更加個人化和專業
