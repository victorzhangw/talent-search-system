# 意圖識別（Use Case）系統說明

## 📋 意圖識別類型

系統目前支援 **4 種意圖識別類型**：

### 1. UC-SEL-01：人才適配性

**描述**：評估候選人是否適合特定職位或角色

**關鍵字**：
- 勝任
- 適合
- 符合
- 能否擔任
- 適配度

**回答風格**：CONSULTING-GRADE（顧問級）
- 使用高/中高/中/低等級描述
- 不得逐分解讀分數
- 檢索證據僅限系統內部

**範例問題**：
- 「這位候選人適合擔任專案經理嗎？」
- 「他能勝任這個職位嗎？」
- 「適配度如何？」

---

### 2. UC-CMP-01：候選人比較

**描述**：比較多位候選人的優劣勢

**關鍵字**：
- 比較
- 差異
- 哪位更好
- 優劣

**回答風格**：ANALYTICAL-COMPARISON（分析比較）
- 保持中立
- 基於事實比較，避免主觀偏好

**範例問題**：
- 「比較這兩位候選人的差異」
- 「哪位更適合這個職位？」
- 「他們的優劣勢是什麼？」

---

### 3. UC-DEV-01：發展建議

**描述**：提供候選人的發展與改進建議

**關鍵字**：
- 發展
- 改進
- 建議
- 缺點
- 弱點

**回答風格**：COACHING-GROWTH（教練成長）
- 使用正向語言（Constructive Feedback）
- 聚焦於行為改變而非人格缺陷

**範例問題**：
- 「這位候選人有哪些發展空間？」
- 「如何改進他的弱點？」
- 「有什麼建議可以幫助他成長？」

---

### 4. UC-GENERAL：通用 HR 助手（Fallback）

**描述**：通用問答，不屬於上述特定類別

**關鍵字**：（無特定關鍵字，作為 fallback）

**回答風格**：EXPLANATORY MODE（說明模式）
- 以說明與理解為目的
- 不進行決策或價值判斷
- 使用程度描述語言（偏高／中等／偏低）
- 避免逐分解讀分數
- 不得提供錄取、淘汰、晉升或風險判斷建議

**範例問題**：
- 「與團隊合作的適配性如何？」
- 「他的特質分析結果是什麼？」
- 「這個分數代表什麼意思？」

---

## 🔍 意圖識別邏輯

### 匹配規則

**檔案**：`services/rag_engine.py` → `_get_use_case()`

**邏輯**：
```python
def _get_use_case(self, query: str) -> Dict[str, Any]:
    """ Hybrid Intent Router """
    # 1. 遍歷所有 use cases
    for uc_id, uc_data in self.use_cases.items():
        # 2. 檢查關鍵字是否在查詢中
        for keyword in uc_data.get('keywords', []):
            if keyword in query:
                return uc_id, uc_data
    
    # 3. 沒有匹配到任何關鍵字，返回 UC-GENERAL
    return "UC-GENERAL", self.use_cases["UC-GENERAL"]
```

### 優先順序

1. **UC-SEL-01**（人才適配性）- 優先匹配
2. **UC-CMP-01**（候選人比較）
3. **UC-DEV-01**（發展建議）
4. **UC-GENERAL**（通用）- Fallback

**注意**：匹配順序取決於 `use_cases.json` 中的定義順序

---

## 📝 Prompt Log 記錄

### 功能說明

系統會自動記錄每次 Chat 請求的完整 Prompt，包括：
- 時間戳記
- Session ID
- **意圖識別（Use Case ID）** ← 重要！
- System Prompt（完整的 RAG context）
- User Query（使用者問題）

### Log 檔案位置

```
BackEnd/api_v2/logs/prompts.log
```

### Log 格式

```
============================================================
TIME: 2026-01-15 13:54:35
SESSION: a1b2c3d4-e5f6-7890-abcd-ef1234567890 | USE_CASE: UC-GENERAL
============================================================
[SYSTEM PROMPT]
你是一個專業的人才測評顧問助手。
...（完整的 RAG context）...

[USER QUERY]
與團隊合作的適配性如何？
============================================================
```

### 實作位置

**檔案**：`services/rag_engine.py`

**函數**：`_log_prompt(session_id, uc_id, sys_prompt, user_query)`

**呼叫時機**：每次呼叫 LLM 之前（第 337 行）

```python
def _call_llm(self, sys_prompt, query, uc_id, session_id="unknown"):
    # Log the prompt before calling
    self._log_prompt(session_id, uc_id, sys_prompt, query)  # ← 這裡記錄
    
    # ... 呼叫 LLM ...
```

---

## 🔧 如何查看 Prompt Log

### 方法 1：直接查看檔案

```bash
# 查看最新的 log
tail -n 100 BackEnd/api_v2/logs/prompts.log

# 搜尋特定 Use Case
grep "USE_CASE: UC-GENERAL" BackEnd/api_v2/logs/prompts.log

# 搜尋特定 Session
grep "SESSION: a1b2c3d4" BackEnd/api_v2/logs/prompts.log
```

### 方法 2：使用文字編輯器

直接打開 `BackEnd/api_v2/logs/prompts.log`

### 方法 3：程式化讀取

```python
import os

log_file = 'BackEnd/api_v2/logs/prompts.log'
with open(log_file, 'r', encoding='utf-8') as f:
    content = f.read()
    print(content)
```

---

## 📊 Use Case 統計分析

### 查看各 Use Case 的使用頻率

```bash
# Windows PowerShell
Get-Content BackEnd\api_v2\logs\prompts.log | Select-String "USE_CASE:" | Group-Object | Select-Object Count, Name
```

```bash
# Linux/Mac
grep "USE_CASE:" BackEnd/api_v2/logs/prompts.log | sort | uniq -c
```

### 預期輸出

```
15 USE_CASE: UC-GENERAL
5  USE_CASE: UC-SEL-01
3  USE_CASE: UC-CMP-01
2  USE_CASE: UC-DEV-01
```

---

## 🎯 意圖識別歸類建議

### 當前歸類邏輯

| Use Case | 主要用途 | 決策性質 | 風險等級 |
|----------|---------|---------|---------|
| **UC-SEL-01** | 選才決策 | 高決策性 | 高風險 |
| **UC-CMP-01** | 候選人比較 | 高決策性 | 高風險 |
| **UC-DEV-01** | 發展建議 | 中決策性 | 中風險 |
| **UC-GENERAL** | 通用說明 | 低決策性 | 低風險 |

### 建議的改進方向

#### 1. 更細緻的分類

**建議新增**：
- `UC-TEAM-01`：團隊協作分析
- `UC-LEAD-01`：領導力評估
- `UC-RISK-01`：風險識別
- `UC-CULTURE-01`：文化適配

#### 2. 多層級分類

```
決策類（High-Stakes）
├── UC-SEL-01：人才適配性
├── UC-CMP-01：候選人比較
└── UC-RISK-01：風險識別

發展類（Growth-Oriented）
├── UC-DEV-01：發展建議
└── UC-COACH-01：教練指導

說明類（Informational）
├── UC-GENERAL：通用說明
└── UC-EXPLAIN-01：特質解釋
```

#### 3. 動態權重調整

根據關鍵字的重要性給予不同權重：

```python
def _get_use_case_weighted(self, query: str):
    scores = {}
    for uc_id, uc_data in self.use_cases.items():
        score = 0
        for keyword, weight in uc_data.get('weighted_keywords', {}).items():
            if keyword in query:
                score += weight
        scores[uc_id] = score
    
    # 返回分數最高的 use case
    best_uc = max(scores, key=scores.get)
    return best_uc if scores[best_uc] > 0 else "UC-GENERAL"
```

---

## 📈 監控與優化

### 建議監控指標

1. **Use Case 分布**
   - 各 Use Case 的使用頻率
   - 是否有過度集中在 UC-GENERAL

2. **匹配準確度**
   - 人工檢查 log，確認意圖識別是否正確
   - 收集錯誤分類的案例

3. **關鍵字覆蓋率**
   - 哪些問題無法被正確分類
   - 需要新增哪些關鍵字

### 優化建議

1. **定期檢查 Prompt Log**
   - 每週檢查一次
   - 找出常見的錯誤分類

2. **更新關鍵字庫**
   - 根據實際使用情況新增關鍵字
   - 移除無效的關鍵字

3. **A/B 測試**
   - 測試不同的 Prompt 模板
   - 比較回答品質

---

## 🔍 Debug 方法

### 檢查意圖識別結果

**前端 Console**（已移除顯示，但仍會傳送）：
```javascript
// 在 ChatContainer.vue 的 sendMessage 中
console.log('Intent:', msg.intent)
```

**後端 Log**：
```
[RAG] Detected Use Case: UC-GENERAL
```

**Prompt Log**：
```
SESSION: xxx | USE_CASE: UC-GENERAL
```

### 測試不同問題的分類

```python
# 在 Python Console 中測試
from services.rag_engine import RAGService

rag = RAGService()

# 測試問題
queries = [
    "這位候選人適合擔任專案經理嗎？",  # 應該是 UC-SEL-01
    "比較這兩位候選人",                # 應該是 UC-CMP-01
    "有什麼發展建議？",                # 應該是 UC-DEV-01
    "與團隊合作的適配性如何？"          # 應該是 UC-GENERAL
]

for q in queries:
    uc_id, uc_data = rag._get_use_case(q)
    print(f"Query: {q}")
    print(f"Use Case: {uc_id} - {uc_data['description']}\n")
```

---

## 📝 總結

### 意圖識別系統

- ✅ 4 種 Use Case 類型
- ✅ 基於關鍵字匹配
- ✅ UC-GENERAL 作為 Fallback
- ✅ 自動記錄到 Prompt Log

### Prompt Log

- ✅ 自動記錄每次請求
- ✅ 包含 Use Case ID
- ✅ 儲存在 `logs/prompts.log`
- ✅ 方便檢查和除錯

### 下一步優化

1. 定期檢查 Prompt Log
2. 收集錯誤分類案例
3. 更新關鍵字庫
4. 考慮實作更細緻的分類系統
