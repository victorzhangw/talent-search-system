# Render 前後端完整部署指南

## 🎯 目標

在 Render 上部署完整的前後端應用（統一平台管理）

---

## 📦 架構

```
Render 平台
├── 後端 Web Service (Python/FastAPI)
│   └── https://talent-search-backend.onrender.com
│
└── 前端 Static Site (Vue 3)
    └── https://talent-search-frontend.onrender.com
```

---

## 🚀 部署步驟

### 第一步：部署後端（已完成）

✅ 你的後端應該已經在 Render 上運行

- 服務名稱：`talent-search-backend`（或你設定的名稱）
- URL：`https://your-backend.onrender.com`

如果還沒部署，請先完成後端部署。

---

### 第二步：部署前端

#### 1. 登入 Render

- 前往 https://render.com
- 使用你的帳號登入

#### 2. 創建 Static Site

- 點擊右上角 **"New +"** 按鈕
- 選擇 **"Static Site"**

#### 3. 連接 Repository

- 選擇你的 Git repository
- 如果看不到，點擊 "Configure account" 授權訪問

#### 4. 配置設定

填寫以下資訊：

| 欄位                  | 填入值                         |
| --------------------- | ------------------------------ |
| **Name**              | `talent-search-frontend`       |
| **Branch**            | `main`                         |
| **Root Directory**    | `frontend`                     |
| **Build Command**     | `npm install && npm run build` |
| **Publish Directory** | `dist`                         |

#### 5. 展開 Advanced 設定

點擊 **"Advanced"** 按鈕，新增環境變數：

**環境變數 1:**

- Key: `NODE_VERSION`
- Value: `18`

**環境變數 2:**

- Key: `VITE_API_BASE_URL`
- Value: `https://your-backend.onrender.com`

  ⚠️ **重要：替換為你的實際後端 URL！**

#### 6. 設定 Rewrite 規則

在 "Redirects/Rewrites" 區域：

- 點擊 **"Add Rule"**
- Source: `/*`
- Destination: `/index.html`
- Action: 選擇 **"Rewrite"**

這是為了支援 Vue Router 的 history 模式。

#### 7. 創建 Static Site

- 點擊 **"Create Static Site"** 按鈕
- 等待建置完成（約 3-5 分鐘）

#### 8. 查看部署狀態

- 在 Dashboard 可以看到建置進度
- 點擊 "Logs" 查看詳細日誌
- 建置成功後會顯示綠色的 "Live" 狀態

---

### 第三步：更新後端 CORS

前端部署完成後，需要更新後端允許的來源。

#### 1. 複製前端 URL

- 例如：`https://talent-search-frontend.onrender.com`

#### 2. 更新後端程式碼

在 `BackEnd/start_fixed_api.py` 中找到 CORS 設定：

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://talent-search-frontend.onrender.com",  # 加入你的前端 URL
        "http://localhost:3000",  # 保留本地開發
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

或暫時允許所有來源（僅用於測試）：

```python
allow_origins=["*"]
```

#### 3. 提交並推送

```bash
git add BackEnd/start_fixed_api.py
git commit -m "更新 CORS 允許前端域名"
git push
```

#### 4. 等待後端重新部署

- Render 會自動偵測並重新部署後端
- 等待約 2-3 分鐘

---

## 🧪 測試部署

### 1. 訪問前端

- 開啟前端 URL：`https://talent-search-frontend.onrender.com`

### 2. 開啟開發者工具

- 按 F12
- 切換到 "Console" 標籤

### 3. 測試功能

- 輸入搜索查詢
- 檢查是否有錯誤訊息
- 確認 API 請求成功

### 4. 檢查 Network

- 切換到 "Network" 標籤
- 查看 API 請求
- 確認狀態碼為 200

---

## ⚠️ 常見問題

### 問題 1：白屏或 404

**原因：** Rewrite 規則未設定

**解決：**

1. 進入 Static Site 設定
2. 找到 "Redirects/Rewrites"
3. 新增規則：`/*` → `/index.html` (Rewrite)

### 問題 2：API 請求失敗（CORS 錯誤）

**錯誤訊息：**

```
Access to fetch at 'https://...' from origin 'https://...' has been blocked by CORS policy
```

**解決：**

1. 檢查後端 CORS 設定
2. 確認前端 URL 已加入 `allow_origins`
3. 重新部署後端

### 問題 3：環境變數未生效

**原因：** 環境變數必須以 `VITE_` 開頭

**解決：**

1. 確認變數名稱為 `VITE_API_BASE_URL`
2. 修改後需要重新部署
3. 在 Dashboard 點擊 "Manual Deploy"

### 問題 4：後端休眠

**現象：** 首次訪問很慢（30-60 秒）

**原因：** Render 免費方案會在閒置 15 分鐘後休眠

**解決方案：**

- 升級到付費方案（$7/月）
- 使用外部服務定期 ping 後端（如 UptimeRobot）
- 接受首次訪問較慢的限制

---

## 📊 監控和管理

### 查看日誌

**前端日誌：**

1. 進入 Static Site Dashboard
2. 點擊 "Logs"
3. 查看建置和部署日誌

**後端日誌：**

1. 進入 Web Service Dashboard
2. 點擊 "Logs"
3. 查看運行時日誌

### 手動重新部署

**前端：**

1. 進入 Static Site
2. 點擊 "Manual Deploy"
3. 選擇 "Clear build cache & deploy"

**後端：**

1. 進入 Web Service
2. 點擊 "Manual Deploy"
3. 選擇 "Deploy latest commit"

---

## 🔄 自動部署

Render 會自動監聽 Git repository：

- ✅ 每次 push 到 `main` 分支
- ✅ 自動觸發重新部署
- ✅ 可以在設定中關閉自動部署

---

## 💰 費用說明

### 免費方案包含：

**前端（Static Site）：**

- ✅ 完全免費
- ✅ 100GB 頻寬/月
- ✅ 自動 HTTPS
- ✅ 全球 CDN

**後端（Web Service）：**

- ✅ 免費
- ⚠️ 閒置 15 分鐘後休眠
- ⚠️ 每月 750 小時運行時間

### 升級選項：

如果需要後端不休眠：

- **Starter Plan**: $7/月
- 24/7 運行
- 更多資源

---

## ✅ 完成檢查清單

- [ ] 後端已部署並可訪問
- [ ] 前端已部署並可訪問
- [ ] 環境變數 `VITE_API_BASE_URL` 已設定
- [ ] Rewrite 規則已設定
- [ ] 後端 CORS 已更新
- [ ] 測試搜索功能正常
- [ ] 沒有 Console 錯誤
- [ ] API 請求成功

---

## 🎉 完成！

你現在有：

- **前端**: `https://talent-search-frontend.onrender.com`
- **後端**: `https://talent-search-backend.onrender.com`

兩個服務都在 Render 上，統一管理！

---

## 📞 需要幫助？

- Render 文檔：https://render.com/docs
- Render 社群：https://community.render.com
- 本專案文檔：查看其他 `.md` 檔案
