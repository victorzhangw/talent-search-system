# 🎉 問題診斷報告

## ✅ 成功獲取資料！

根據後端日誌，API 已經成功：
- ✅ 接收到請求：`assessment_ids: [62, 63]`
- ✅ 呼叫上游 API 成功
- ✅ 獲取到 2 個報告
- ✅ 格式化並回傳

---

## 🐛 發現的問題

### 問題 1: assessment_id 不匹配

**上游 API 的資料結構**：
```json
{
  "assessment_id": 58,  ← 這是 candidate_id（誤導性的欄位名稱）
  "ok": true,
  "assessment": {
    "assessment_id": 62,  ← 這才是真正的 assessment_id！
    "project_code": 3,
    "trait_results": { ... }
  }
}
```

**問題**：
- 前端傳送：`[62, 63]`（assessment_id）
- 後端原本提取：`58, 59`（candidate_id，因為取錯了欄位）
- 導致前端無法匹配到正確的候選人

**已修正**：
- 現在優先從 `assessment.assessment_id` 提取
- 這樣就會正確提取到 `62, 63`

---

### 問題 2: 可能的重複呼叫

你提到「最後還呼叫一次 API」，這可能是因為：

#### 可能原因 A: 前端有錯誤導致重試
檢查前端 Console 是否有錯誤訊息

#### 可能原因 B: 瀏覽器的 preflight 請求
OPTIONS 請求（CORS preflight）會在 POST 之前發送，但這不會執行我們的程式碼

#### 可能原因 C: 前端程式碼被呼叫兩次
檢查 `handleSelectionConfirmed` 是否被呼叫兩次

---

## 🔧 下一步行動

### 1. 重新啟動後端
```bash
# 在後端 terminal 按 Ctrl+C
# 然後重新執行
python app.py
```

### 2. 清空瀏覽器快取
- F12 → Application → Clear site data
- 或 Ctrl+Shift+Delete

### 3. 重新測試
1. 重新整理頁面（Ctrl+F5）
2. 選擇候選人
3. 點擊「開始提問」
4. **檢查前端 Console**

### 4. 檢查前端 Console 日誌

應該看到：
```
[ChatContainer] ========== Batch Report Fetch Debug ==========
[ChatContainer] Selected candidates: Array(2)
[ChatContainer] Extracted assessment IDs: [62, 63]
[ChatContainer] API URL: http://localhost:5000/api/v2/reports/batch
[ChatContainer] Request Payload: {
  "assessment_ids": [62, 63]
}
[ChatContainer] Response status: 200 OK
[ChatContainer] Response data: {reports: Array(2)}
[ChatContainer] Number of reports received: 2
[ChatContainer] Processing report for assessment_id: 62
[ChatContainer] Matched to candidate_id: 58
[ChatContainer] Processing report for assessment_id: 63
[ChatContainer] Matched to candidate_id: 59
[ChatContainer] Final reportsMap: {58: {...}, 59: {...}}
[ChatContainer] Number of reports to save: 2
[ChatContainer] ✅ Saved to Session Storage
```

### 5. 檢查 Session Storage

- F12 → Application → Session Storage → localhost:5173
- 查看 `traitty_batch_reports`
- 應該有內容：
```json
{
  "58": {
    "assessment_id": 62,
    "traits": [...],
    "assessment_date": "..."
  },
  "59": {
    "assessment_id": 63,
    "traits": [...],
    "assessment_date": "..."
  }
}
```

---

## 📊 預期的後端日誌

修正後，應該看到：
```
[Batch Reports] Report 1 assessment_id: 62  ← 正確！
🔥 DEBUG: Top-level assessment_id: 58, Nested assessment_id: 62
[Batch Reports] Report 1 has 35 traits
[Batch Reports] Report 1 formatted with 35 traits

[Batch Reports] Report 2 assessment_id: 63  ← 正確！
🔥 DEBUG: Top-level assessment_id: 59, Nested assessment_id: 63
[Batch Reports] Report 2 has 35 traits
[Batch Reports] Report 2 formatted with 35 traits
```

---

## 🎯 關於「重複呼叫」的診斷

### 檢查方法 1: 計算呼叫次數

在前端 Console 中執行：
```javascript
let callCount = 0
const originalFetch = window.fetch
window.fetch = function(...args) {
  if (args[0].includes('/reports/batch')) {
    callCount++
    console.log(`🔥 Batch API called ${callCount} times`)
    console.trace('Call stack:')
  }
  return originalFetch.apply(this, args)
}
```

### 檢查方法 2: 查看 Network 標籤

- F12 → Network
- 清空記錄
- 點擊「開始提問」
- 查看有幾個 `/reports/batch` 請求

### 可能的解決方案

如果確實有重複呼叫，可能需要：

1. **添加防抖（debounce）**：
```javascript
let isFetching = false

const fetchBatchTraitReports = async (selectedCandidates) => {
    if (isFetching) {
        console.warn('[ChatContainer] Already fetching, skipping...')
        return
    }
    
    isFetching = true
    try {
        // ... 原有邏輯 ...
    } finally {
        isFetching = false
    }
}
```

2. **檢查是否有多個事件監聽器**：
確保「開始提問」按鈕沒有綁定多個 click 事件

---

## 📝 總結

### 已修正
- ✅ assessment_id 提取邏輯（現在會正確提取 62, 63 而非 58, 59）

### 待確認
- ⏳ 前端是否成功儲存到 Session Storage
- ⏳ 是否真的有重複呼叫（需要查看前端 Console 和 Network）

### 下一步
1. 重新啟動後端
2. 重新測試
3. 提供前端 Console 的完整日誌
4. 檢查 Session Storage 的內容
