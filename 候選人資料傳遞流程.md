# 候選人資料傳遞流程說明

## 📊 完整資料流程圖

```
前端選擇候選人
    ↓
儲存到 Session Storage (複用)
    ↓
點擊「開始提問」→ 進入 Chat 模式
    ↓
使用者輸入問題並發送
    ↓
前端發送 POST /chat/ 請求
    ↓
Payload 包含完整候選人資料
    ↓
後端接收並優先使用前端資料
    ↓
提取 assessment_ids
    ↓
批次呼叫 /v1/assessments/latest:batch
    ↓
合併候選人基本資料 + 評估報告
    ↓
建立 RAG Context
    ↓
呼叫 LLM 生成回應
    ↓
串流回傳給前端
```

---

## 🔄 前端 Payload 結構

### 當使用者點擊「開始提問」並發送訊息時

**API Endpoint**: `POST /chat/`

**Request Payload**:
```json
{
  "query": "與團隊合作的適配性如何？",
  "candidate_ids": [58, 59, 60, 57],
  "candidates_info": [
    {
      "candidate_id": 58,
      "name": "吳至平",
      "email": "peterwuzhp@outlook.com",
      "phone": "",
      "enterprise_name": "WePredict",
      "position": "",
      "status": "employed",
      "created_at": "2025-12-01T14:18:37.437348Z",
      "last_assessment_date": "2025-12-03T09:30:48.315395Z",
      "latest_assessment": {
        "assessment_id": 62,
        "project_code": 3,
        "project_name": "CIA綜合人才洞察評鑑",
        "score_value": 56
      }
    },
    {
      "candidate_id": 59,
      "name": "阮暐捷",
      "email": "svtcarat224@gmail.com",
      "phone": "",
      "enterprise_name": "WePredict",
      "position": "",
      "status": "employed",
      "created_at": "2025-12-01T14:18:37.445446Z",
      "last_assessment_date": "2025-12-03T09:30:44.494004Z",
      "latest_assessment": {
        "assessment_id": 63,
        "project_code": 3,
        "project_name": "CIA綜合人才洞察評鑑",
        "score_value": 62
      }
    }
    // ... 其他候選人
  ],
  "session_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
}
```

---

## 🔑 關鍵參數說明

### 1. `candidate_ids` (Array of Numbers)
- **用途**: 候選人 ID 列表
- **來源**: `selectedCandidateIds.value`
- **範例**: `[58, 59, 60, 57]`

### 2. `candidates_info` (Array of Objects)
- **用途**: 完整的候選人基本資料（避免後端重新從 API 獲取）
- **來源**: `selectedCandidatesObjects.value`（從前端已載入的 `candidates.value` 過濾）
- **包含欄位**:
  - `candidate_id`: 候選人 ID
  - `name`: **真實姓名**（關鍵！）
  - `email`: 電子郵件
  - `phone`: 電話
  - `enterprise_name`: 企業名稱
  - `position`: 職位
  - `status`: 狀態
  - `created_at`: 建立時間
  - `last_assessment_date`: 最後評估日期
  - `latest_assessment`: **最新評估資訊**（包含 `assessment_id`）

### 3. `session_id` (String - UUID)
- **用途**: 會話識別碼，用於 RAG cache 隔離
- **來源**: `currentSessionId.value`
- **特性**: 每次「重選」時會重新生成

---

## 🎯 後端處理流程

### Step 1: 接收 Payload
**檔案**: `BackEnd/api_v2/routes/chat.py`

```python
data = request.json
query = data.get('query')
candidate_ids = data.get('candidate_ids', [])
candidates_info = data.get('candidates_info', [])  # 完整候選人資料
session_id = data.get('session_id', 'default_session')
```

### Step 2: 優先使用前端資料
**檔案**: `BackEnd/api_v2/services/rag_engine.py`

```python
if candidates_info:
    # 使用前端提供的資料（優先）
    print(f"[RAG] Using {len(candidates_info)} candidates from frontend")
    target_candidates_basic = candidates_info
else:
    # 回退：從 Traitty API 獲取
    print(f"[RAG] No frontend candidate info. Fetching from API...")
    # ... API 獲取邏輯 ...
```

### Step 3: 提取 Assessment IDs
```python
assessment_ids = []
for cand in target_candidates_basic:
    lat = cand.get('latest_assessment')
    if lat and isinstance(lat, dict) and 'assessment_id' in lat:
        aid = lat['assessment_id']
        assessment_ids.append(aid)
```

**提取結果**: `[62, 63, 64, 61]`

### Step 4: 批次獲取評估報告
**API Endpoint**: `POST https://uat.traitty.com/v1/assessments/latest:batch`

**Request Body**:
```json
{
  "assessment_ids": ["62", "63", "64", "61"]
}
```

**Response**: 包含每個 assessment 的詳細 `trait_results`

### Step 5: 合併資料
```python
for cand in target_candidates_basic:
    my_asmt_id = cand.get('latest_assessment', {}).get('assessment_id')
    
    # 從批次結果中找到對應的評估報告
    my_asmt_data = None
    for a in assessments_list:
        if str(a.get('assessment_id')) == str(my_asmt_id):
            my_asmt_data = a
            break
    
    merged = cand.copy()
    if my_asmt_data:
        merged['assessment'] = my_asmt_data.get('assessment', {})
    
    final_candidates_data.append(merged)
```

### Step 6: 建立 RAG Context
**檔案**: `BackEnd/api_v2/services/context_builder.py`

使用候選人的 **真實姓名** 建立 prompt：
```python
name = cand.get('name') or f"Candidate-{cand.get('candidate_id', 'Unknown')}"
position = cand.get('position', 'NA')
cand_name = f"{name} ({position})"

components["base_analysis"] += f"### 候選人: {cand_name}\n"
```

**輸出範例**:
```
### 候選人: 吳至平 (NA)
  * [ANI_01] 同理心 (Score 61 -> Band Medium):
    - 語意: 中等同理心
    - 描述: 能理解他人感受...
```

---

## 💾 Session Storage 管理

### 儲存時機
**觸發**: 使用者確認選擇候選人時（點擊「開始提問」）

**程式碼**:
```javascript
const handleSelectionConfirmed = (ids) => {
    const selectedCandidates = candidates.value.filter(c => ids.includes(c.candidate_id))
    sessionStorage.setItem('traitty_selected_candidates', JSON.stringify(selectedCandidates))
}
```

### 清除時機
1. **點擊「重選」按鈕**
2. **回到選擇候選人 UI**

**程式碼**:
```javascript
const resetAndReselect = () => {
    sessionStorage.removeItem('traitty_selected_candidates')
    // ... 其他重置邏輯 ...
}
```

### 複用場景（未來可擴展）
- 頁面重新整理後恢復選擇狀態
- 在不同 tab 之間切換時保持選擇
- 快速重新開始對話（不需重新選擇）

---

## ✅ 驗證檢查清單

### 前端檢查
- [ ] `candidates_info` 包含完整的候選人資料
- [ ] `candidates_info[].name` 不為空
- [ ] `candidates_info[].latest_assessment.assessment_id` 存在
- [ ] Session Storage 正確儲存和清除

### 後端檢查
- [ ] 收到 `candidates_info` 參數
- [ ] 優先使用 `candidates_info`（不從 API 重新獲取）
- [ ] 正確提取 `assessment_ids`
- [ ] 批次 API 呼叫成功
- [ ] Context Builder 使用真實姓名

### 日誌檢查
```
[RAG] Using 4 candidates from frontend
[RAG-DEBUG] Frontend candidate: id=58, name='吳至平', position=''
[RAG-DEBUG] Frontend candidate: id=59, name='阮暐捷', position=''
[RAG] Prepared Assessment IDs: [62, 63, 64, 61]
[RAG] Fetched 4 assessments.
[ContextBuilder-DEBUG] Processing candidate: 58, name='吳至平', position=''
```

---

## 🔧 Debug 方法

### 前端 Console
```javascript
// 檢查選中的候選人資料
console.log('Selected Candidates:', selectedCandidatesObjects.value)

// 檢查 Session Storage
console.log('Session Storage:', sessionStorage.getItem('traitty_selected_candidates'))

// 檢查發送的 Payload
console.log('Chat Payload:', {
  candidate_ids: selectedCandidateIds.value,
  candidates_info: selectedCandidatesObjects.value
})
```

### 後端 Log
```python
# 在 rag_engine.py 中
print(f"[RAG-DEBUG] Received candidates_info: {candidates_info}")
print(f"[RAG-DEBUG] Assessment IDs: {assessment_ids}")
```

---

## 📝 總結

### 資料傳遞參數
- **主要參數**: `candidates_info`
- **資料來源**: 前端已載入的 `candidates.value`
- **關鍵欄位**: `name`、`latest_assessment.assessment_id`

### 批次 API 呼叫
- **Endpoint**: `POST /v1/assessments/latest:batch`
- **參數**: `assessment_ids` (從 `latest_assessment.assessment_id` 提取)
- **用途**: 獲取每位候選人的詳細特質報告

### Session Storage
- **儲存**: 選擇候選人後
- **清除**: 點擊重選或回到選擇頁面
- **用途**: 快取選中的候選人資料供複用
