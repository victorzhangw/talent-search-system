# 批次報告 API 除錯檢查清單

## 🔍 立即檢查項目

### 1. 前端瀏覽器 Console 檢查

打開瀏覽器開發者工具（F12），切換到 **Console** 標籤，然後：

1. 清空 Console（點擊 🚫 圖示）
2. 重新選擇候選人
3. 點擊「開始提問」
4. **立即查看 Console 輸出**

#### 應該看到的日誌：

```
[ChatContainer] ========== Batch Report Fetch Debug ==========
[ChatContainer] Selected candidates: Array(4) [...]
[ChatContainer] Extracted assessment IDs: [62, 63, 64, 61]
[ChatContainer] API URL: http://localhost:5000/api/v2/reports/batch
[ChatContainer] Request Payload: {
  "assessment_ids": [62, 63, 64, 61]
}
[ChatContainer] Authorization: Token present
```

#### 如果沒有看到這些日誌：

**可能原因 A：前端沒有重新編譯**
- 解決方案：
  1. 停止前端服務（Ctrl+C）
  2. 重新執行 `.\start-v2-frontend.bat`
  3. 等待 Vite 編譯完成
  4. 重新整理瀏覽器（Ctrl+F5 強制重新整理）

**可能原因 B：`fetchBatchTraitReports` 沒有被呼叫**
- 檢查方法：在 Console 中執行
  ```javascript
  console.log(typeof fetchBatchTraitReports)
  ```
  如果顯示 `undefined`，表示函數沒有正確定義

**可能原因 C：有 JavaScript 錯誤**
- 檢查 Console 是否有紅色錯誤訊息
- 點擊錯誤查看詳細資訊

---

### 2. 後端 Console 檢查

查看後端的 terminal 視窗，應該看到：

```
============================================================
[Batch Reports] ========== Request Received ==========
[Batch Reports] Authorization header: Bearer eyJhbG...
[Batch Reports] Request body: {'assessment_ids': [62, 63, 64, 61]}
[Batch Reports] Assessment IDs: [62, 63, 64, 61]
```

#### 如果只看到：

```
127.0.0.1 - - [15/Jan/2026 12:57:57] "POST /api/v2/reports/batch HTTP/1.1" 200 -
```

**這表示：**
- ✅ API 有被呼叫
- ✅ 回應成功（200）
- ❌ **但是沒有執行我們的 debug 程式碼**

**可能原因：Flask 沒有重新載入程式碼**

**解決方案：**
1. **完全停止後端服務**（在後端 terminal 按 Ctrl+C）
2. **確認服務已停止**（應該看到 "^C" 或返回命令提示符）
3. **重新啟動後端**：
   ```bash
   cd BackEnd\api_v2
   .venv\Scripts\activate
   python app.py
   ```
4. **等待看到**：
   ```
   * Running on http://127.0.0.1:5000
   * Restarting with stat
   ```
5. **重新測試**

---

### 3. 網路請求檢查

在瀏覽器開發者工具中：

1. 切換到 **Network** 標籤
2. 清空記錄
3. 點擊「開始提問」
4. 找到 `batch` 請求
5. 點擊查看詳細資訊

#### 檢查項目：

**Request Headers**：
```
POST /api/v2/reports/batch HTTP/1.1
Content-Type: application/json
Authorization: Bearer eyJ...
```

**Request Payload**：
```json
{
  "assessment_ids": [62, 63, 64, 61]
}
```

**Response**：
```json
{
  "reports": [...]
}
```

#### 如果 Request Payload 是空的或沒有 assessment_ids：

**問題：前端沒有正確傳送資料**

**檢查前端程式碼**：
```javascript
// 在 ChatContainer.vue 的 fetchBatchTraitReports 函數中
const payload = { assessment_ids: assessmentIds }
console.log('[DEBUG] Payload to send:', payload)
```

---

## 🚨 最可能的問題

根據你的描述「沒有看到傳入的 payload」，最可能的情況是：

### 問題：後端程式碼沒有重新載入

**症狀**：
- API 回應 200（成功）
- 但是沒有顯示 debug 日誌
- 表示還在執行舊的程式碼

**解決方案**：

#### 方法 1：手動重啟後端（推薦）

1. 找到後端的 terminal 視窗（標題：TalentSearch V2 - Backend）
2. 按 **Ctrl+C** 停止服務
3. 等待完全停止
4. 重新執行：
   ```bash
   python app.py
   ```
5. 等待看到 "Running on http://127.0.0.1:5000"
6. 重新測試

#### 方法 2：修改 app.py 強制重新載入

在 `BackEnd/api_v2/app.py` 的最後，確認有：

```python
if __name__ == '__main__':
    app = create_app()
    app.run(host='0.0.0.0', port=5000, debug=True, use_reloader=True)
```

確保 `debug=True` 和 `use_reloader=True`

---

## 📝 快速診斷步驟

### Step 1: 確認前端有傳送請求

在前端 Console 執行：

```javascript
// 檢查 API URL 配置
const { apiBaseUrl } = getApiConfig()
console.log('API Base URL:', apiBaseUrl)

// 手動測試 API 呼叫
fetch(`${apiBaseUrl}/reports/batch`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${userToken.value}`
  },
  body: JSON.stringify({
    assessment_ids: [62, 63]
  })
})
.then(res => res.json())
.then(data => console.log('API Response:', data))
.catch(err => console.error('API Error:', err))
```

### Step 2: 確認後端有收到資料

在後端 `reports.py` 的 `get_batch_reports` 函數**最開頭**添加：

```python
@bp.route('/batch', methods=['POST'])
def get_batch_reports():
    import sys
    print("=" * 60, file=sys.stderr, flush=True)
    print("🔥 BATCH REPORTS API CALLED!", file=sys.stderr, flush=True)
    print(f"Request data: {request.get_json()}", file=sys.stderr, flush=True)
    print("=" * 60, file=sys.stderr, flush=True)
    
    # ... 原有程式碼 ...
```

使用 `sys.stderr` 可以確保日誌一定會顯示。

---

## 🎯 下一步行動

請按照以下順序執行：

1. ✅ **完全停止前後端服務**（Ctrl+C）
2. ✅ **重新啟動後端**（確認看到 "Running on..." 訊息）
3. ✅ **重新啟動前端**（確認 Vite 編譯完成）
4. ✅ **強制重新整理瀏覽器**（Ctrl+F5）
5. ✅ **清空瀏覽器 Console**
6. ✅ **重新選擇候選人並點擊「開始提問」**
7. ✅ **立即複製前端 Console 的所有輸出**
8. ✅ **立即複製後端 terminal 的所有輸出**

將這兩個 Console 的完整輸出提供給我，我可以幫你精確定位問題！
